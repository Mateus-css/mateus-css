# Images

Add real assets here as you get them, for example:

- `portrait.jpg` — your photo, to replace the "YOUR PHOTO" placeholder in the About section (`index.html`)
- `og-cover.jpg` — social-share preview image referenced by the Open Graph tag in `index.html`
- Any before/after frames or portfolio thumbnails you want to swap in for the current CSS placeholders
